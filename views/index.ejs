<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Joke of the Day</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <div class="container">
      <h1>Joke of the Day</h1>

      <% if (!joke && message) { %>
      <p><%= message %></p>
      <% } %> <% if (joke) { %> <% if (joke.type === 'single') { %>
      <p><%= joke.joke %></p>
      <% } else if (joke.type === 'twopart') { %>
      <p><%= joke.setup %> - <%= joke.delivery %></p>
      <% } %> <% } %>

      <form action="/api/joke" method="get">
        <button type="submit">Get a Joke</button>
      </form>
    </div>
  </body>
</html>
